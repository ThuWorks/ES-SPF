import_regions = function(rasterFiles = list(), shapefiles = list()) {
  
  trondheim = rast(trd)
  trondheim2 = vect(flat)
  trondheim3 = vect(curve)
  
  crs(trondheim3)
  
  plot(trondheim3)
  trondheim3
}